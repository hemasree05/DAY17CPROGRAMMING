#include<stdio.h>
main()
{
	int n,i,h,e,sh;
	printf("enter a number");
	scanf("%d",&n);
	printf("enter the elements\n");
	for(i=1;i<=n;i++)
	{
	scanf("%d",&e);
	  if(e>h)
	    {
	    sh=h;
	  	h=e;
		}
		else if((e>sh)&&(e!=h))
        {
		sh=e;
		}   
	}
    printf("2ndHighest is %d",sh);
}
